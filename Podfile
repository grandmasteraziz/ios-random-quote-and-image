# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'quote with image' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for quote with image
  pod 'SDWebImage', '~> 5.0'

  target 'quote with imageTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'quote with imageUITests' do
    # Pods for testing
  end

end
